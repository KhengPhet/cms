# Base URL of the Express CMS backend API.
# The backend server binds to port 8080 by default (see server.js: PORT || 8080).
# NOTE: no trailing "/api" — the frontend endpoints already include "/api".
VITE_API_URL=http://localhost:8080
