# Base URL of the Express CMS backend API (Railway production).
# The backend server binds to port 8080 by default (see server.js: PORT || 8080).
VITE_API_BASE_URL=https://beneficial-luck-production-efd7.up.railway.app