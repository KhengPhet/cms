# Base URL of the Express CMS backend API (Railway production).
# NOTE: no trailing "/api" here — the frontend endpoints already include "/api"
# (e.g. GET /api/posts), so the base must be the domain root to avoid a double "/api".
VITE_API_URL=https://expressapicms-production.up.railway.app